// writing the test for login endpoint
import { callNext } from '../controllers/http'
// a function tp test the /api/login endpoint
// the function takes in a username and password
// and returns a promise
// the promise is resolved if the login is successful
// and rejected if the login is unsuccessful
